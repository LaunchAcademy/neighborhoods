<h2>Add a New Neighborhood</h2>

<%= simple_form_for(@neighborhood, html: {class: 'form-horizontal' }) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>

  <%= f.button :submit %>
<% end %>
